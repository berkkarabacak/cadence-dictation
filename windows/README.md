# Cadence for Windows
# Hold Ctrl+Win, speak, text is inserted at the caret in the focused app.

See cadence_app.py. Run:

    pip install -r requirements.txt
    python cadence_app.py

Build exe:

    powershell -File build.ps1
